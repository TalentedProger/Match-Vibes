# 🔒 Проблема безопасности: Компрометация токена бота

**Дата обнаружения:** 2025-01-08  
**Статус:** 🔴 КРИТИЧНО - Требуется немедленное действие  
**Тип проблемы:** Утечка токена Telegram Bot

---

## ⚠️ Что произошло?

Вы заметили, что информация о вашем боте @VibesMatch_bot заполнилась **чужими ссылками на сайты**, которые вы не вводили. Это означает, что:

1. **Токен бота был скомпрометирован** - кто-то получил доступ к вашему токену
2. **Третьи лица используют ваш бот** для своих целей
3. **Возможно использование для спама или фишинга**

### Как это могло произойти?

- Токен был случайно закоммичен в публичный Git репозиторий
- Токен был виден в открытых логах или документации
- Токен был отправлен в незащищенном виде (email, мессенджер)
- Доступ к серверу/компьютеру разработчика

---

## 🚨 НЕМЕДЛЕННЫЕ ДЕЙСТВИЯ

### Шаг 1: Создайте новый токен бота (5 минут)

1. Откройте Telegram → найдите **@BotFather**
2. Отправьте команду: `/mybots`
3. Выберите **@VibesMatch_bot**
4. Нажмите **"API Token"**
5. Нажмите **"Revoke current token"** (отозвать текущий)
6. Подтвердите действие
7. **Скопируйте новый токен** (он будет показан один раз!)

**Пример нового токена:**

```
8110389649:AAExxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

### Шаг 2: Обновите токен в проекте

#### 2.1. Локальный проект

Обновите файл `.env.local`:

```env
# Telegram Mini App
NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=8110389649:AAE_НОВЫЙ_ТОКЕН_ЗДЕСЬ
NEXT_PUBLIC_BOT_USERNAME=VibesMatch_bot

# Telegram Bot (Stage 1.5)
TELEGRAM_BOT_TOKEN=8110389649:AAE_НОВЫЙ_ТОКЕН_ЗДЕСЬ
TELEGRAM_BOT_USERNAME=VibesMatch_bot
```

#### 2.2. Vercel (Production)

1. Откройте https://vercel.com/dashboard
2. Выберите ваш проект
3. Перейдите в **Settings** → **Environment Variables**
4. Найдите переменные:
   - `NEXT_PUBLIC_TELEGRAM_BOT_TOKEN`
   - `TELEGRAM_BOT_TOKEN`
5. Нажмите **Edit** → вставьте **новый токен**
6. Сохраните изменения
7. **Redeploy** проект для применения изменений

---

### Шаг 3: Проверьте, что старый токен больше не работает

Запустите команду для проверки:

```bash
# Windows PowerShell
Invoke-WebRequest -Uri "https://api.telegram.org/bot<СТАРЫЙ_ТОКЕН>/getMe"
```

Вы должны увидеть ошибку:

```json
{
  "ok": false,
  "error_code": 401,
  "description": "Unauthorized"
}
```

Это означает, что старый токен отозван ✅

---

### Шаг 4: Перезапустите бота

```bash
# Остановите текущий бот (если запущен)
# Ctrl+C в терминале где запущен бот

# Запустите с новым токеном
pnpm bot:dev
```

---

## 🛡️ Предотвращение в будущем

### Правило 1: НИКОГДА не коммитьте токены в Git

Убедитесь, что `.env.local` в `.gitignore`:

```bash
# Проверьте содержимое .gitignore
cat .gitignore | grep ".env.local"
```

Должно быть:

```
.env.local
.env*.local
```

### Правило 2: Используйте переменные окружения

❌ **Никогда так:**

```typescript
const token = '8110389649:AAExxxxxxxxxx' // BAD!
```

✅ **Всегда так:**

```typescript
const token = process.env.TELEGRAM_BOT_TOKEN // GOOD!
```

### Правило 3: Разные токены для разных окружений

- **Development:** Используйте тестового бота
- **Production:** Используйте основного бота
- **Staging:** Используйте отдельного бота

### Правило 4: Проверьте Git историю

Если токен был закоммичен, удалите его из истории:

```bash
# Проверьте, есть ли токен в истории
git log --all --full-history --source -- .env.local

# Если нашли - очистите историю (ОПАСНО!)
# Лучше создайте новый репозиторий
```

### Правило 5: Используйте GitHub Secrets

Если используете GitHub Actions, храните токены в **Secrets**:

1. GitHub → Repository → Settings
2. Secrets and variables → Actions
3. New repository secret
4. Добавьте `TELEGRAM_BOT_TOKEN`

---

## 📊 Checklist восстановления безопасности

- [ ] Создан новый токен через @BotFather
- [ ] Старый токен отозван
- [ ] Обновлен `.env.local` с новым токеном
- [ ] Обновлены переменные в Vercel
- [ ] Проверено, что старый токен не работает
- [ ] Бот перезапущен с новым токеном
- [ ] Проверено, что `.env.local` в `.gitignore`
- [ ] Проверена Git история на наличие токенов
- [ ] Изменены пароли (если есть подозрение на взлом)

---

## 🔍 Как проверить текущее состояние бота

```bash
# Проверьте информацию о боте
curl "https://api.telegram.org/bot<НОВЫЙ_ТОКЕН>/getMe"
```

Должно вернуться:

```json
{
  "ok": true,
  "result": {
    "id": 8110389649,
    "is_bot": true,
    "first_name": "VibesMatch",
    "username": "VibesMatch_bot"
  }
}
```

---

## 📞 Если проблема сохраняется

1. **Создайте полностью нового бота:**
   - @BotFather → `/newbot`
   - Дайте новое имя (например: `VibesMatchV2_bot`)
   - Используйте новый токен

2. **Проверьте доступы:**
   - Измените пароли к серверам
   - Проверьте логи доступа
   - Проверьте кто имеет доступ к проекту

3. **Свяжитесь с Telegram Support:**
   - Если подозреваете серьезную утечку
   - @BotSupport в Telegram

---

## 📚 Дополнительные ресурсы

- [Telegram Bot API Security](https://core.telegram.org/bots/api#authorizing-your-bot)
- [Vercel Environment Variables](https://vercel.com/docs/concepts/projects/environment-variables)
- [GitHub Secrets](https://docs.github.com/en/actions/security-guides/encrypted-secrets)

---

**ВАЖНО:** После выполнения всех шагов, **удалите старый токен** из всех мест, где он мог быть сохранен (документы, скриншоты, чаты).

**Статус безопасности:** 🔴 → 🟡 → 🟢 (после выполнения всех шагов)
