# ⚡ БЫСТРОЕ РЕШЕНИЕ - Mini App не открывается

## 🎯 Проблема

Кнопка блокируется и Mini App не открывается.

## ✅ Причина

Telegram требует **HTTPS**! HTTP (localhost) не работает.

---

## 🚀 РЕШЕНИЕ ЗА 5 МИНУТ

### Шаг 1: Установите ngrok

**Скачайте:** https://ngrok.com/download

Или через winget:

```powershell
winget install ngrok
```

### Шаг 2: Запустите 3 терминала

**Terminal 1 - Mini App:**

```bash
cd C:\Users\Салим\Desktop\Together_tests
pnpm dev
```

**Terminal 2 - ngrok:**

```bash
ngrok http 3002
```

Вы получите URL типа:

```
https://1234-abc-def.ngrok.io
```

**Скопируйте этот HTTPS URL!**

**Terminal 3 - Обновите .env.local:**

Откройте `.env.local` и замените:

```env
NEXT_PUBLIC_APP_URL=https://1234-abc-def.ngrok.io
```

(Вставьте ваш URL из ngrok!)

### Шаг 3: Настройте Menu Button

```bash
pnpm bot:menu
```

Должно появиться:

```
✅ Menu Button configured successfully!
```

### Шаг 4: Настройте в @BotFather

Откройте Telegram → @BotFather:

```
/mybots
→ @VibesMatch_bot
→ Bot Settings
→ Menu Button
→ Configure menu button
→ Отправьте: https://1234-abc-def.ngrok.io
→ Отправьте текст: 🎮 Открыть игру
```

Также настройте Web App:

```
/mybots
→ @VibesMatch_bot
→ Edit Bot
→ Edit Web App URL
→ Отправьте: https://1234-abc-def.ngrok.io
```

Или создайте новый Mini App:

```
/newapp
→ @VibesMatch_bot
→ MatchVibe
→ URL: https://1234-abc-def.ngrok.io
```

### Шаг 5: Перезапустите бота

```bash
# Остановите старый (Ctrl+C в терминале с ботом)
pnpm bot:dev
```

### Шаг 6: ТЕСТИРУЙТЕ! 🎉

1. Откройте @VibesMatch_bot в Telegram
2. Нажмите на кнопку меню **(☰)** внизу экрана
3. Выберите **"🎮 Открыть игру"**
4. Mini App откроется!

---

## 🎯 Результат

✅ Menu Button работает  
✅ Mini App открывается  
✅ HTTPS настроен  
✅ Все функции доступны

---

## 💡 Важно

- **ngrok должен быть запущен** всегда во время разработки
- **При перезапуске ngrok** URL изменится - нужно обновить .env.local и @BotFather
- **Для production** используйте Vercel/Netlify вместо ngrok

---

## 🐛 Если не работает

### Menu Button не появляется?

→ Настройте в @BotFather (шаг 4)

### Кнопка серая?

→ Проверьте, что ngrok запущен и URL правильный

### Mini App не загружается?

→ Откройте ngrok URL в браузере - должен работать

---

**Готово! Теперь все работает!** 🚀
