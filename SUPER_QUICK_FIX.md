# ⚡ СУПЕР БЫСТРОЕ РЕШЕНИЕ - localtunnel (БЕЗ РЕГИСТРАЦИИ!)

## 🚀 Решение за 2 минуты

### Шаг 1: Запустите Mini App

**Terminal 1:**

```bash
cd C:\Users\Салим\Desktop\Together_tests
pnpm dev
```

Дождитесь:

```
✓ Ready in 2s
○ Local: http://localhost:3002
```

### Шаг 2: Запустите localtunnel

**Terminal 2 (новый терминал):**

```bash
npx localtunnel --port 3002
```

Вы получите URL типа:

```
your url is: https://brave-cat-12.loca.lt
```

**СКОПИРУЙТЕ этот URL!** (например: `https://brave-cat-12.loca.lt`)

⚠️ **Важно:** При первом открытии в браузере нужно подтвердить. Просто откройте URL в браузере и нажмите "Continue".

### Шаг 3: Обновите .env.local

Откройте `.env.local` и замените:

```env
NEXT_PUBLIC_APP_URL=https://brave-cat-12.loca.lt
```

(Вставьте ваш реальный URL из localtunnel!)

### Шаг 4: Настройте Menu Button

**Terminal 3:**

```bash
pnpm bot:menu
```

Должно появиться:

```
✅ Menu Button configured successfully!
```

### Шаг 5: Настройте в @BotFather

Откройте Telegram → @BotFather:

```
/mybots
→ @VibesMatch_bot
→ Bot Settings
→ Menu Button
→ Configure menu button
→ Отправьте: https://brave-cat-12.loca.lt (ваш URL!)
→ Отправьте текст: 🎮 Открыть игру
```

Также настройте Web App:

```
/newapp (если ещё не создавали)
→ @VibesMatch_bot
→ MatchVibe
→ URL: https://brave-cat-12.loca.lt
```

Или обновите существующий:

```
/mybots
→ @VibesMatch_bot
→ Edit Bot
→ Edit Web App URL
→ Отправьте: https://brave-cat-12.loca.lt
```

### Шаг 6: Перезапустите бота

**Terminal 4:**

```bash
pnpm bot:dev
```

### Шаг 7: ТЕСТИРУЙТЕ! 🎉

1. Откройте @VibesMatch_bot в Telegram
2. Отправьте `/start`
3. Нажмите кнопку меню **(☰)** внизу экрана
4. Выберите **"🎮 Открыть игру"**
5. **Mini App откроется!** ✅

---

## 📋 Checklist

- [ ] Terminal 1: `pnpm dev` запущен
- [ ] Terminal 2: `npx localtunnel --port 3002` запущен
- [ ] HTTPS URL скопирован из localtunnel
- [ ] `.env.local` обновлен с новым URL
- [ ] `pnpm bot:menu` выполнен успешно
- [ ] Menu Button настроен в @BotFather
- [ ] Web App URL обновлен в @BotFather
- [ ] Terminal 4: `pnpm bot:dev` запущен
- [ ] Тест в Telegram: Menu Button работает ✅

---

## 💡 Преимущества localtunnel

✅ НЕ требует регистрации  
✅ НЕ требует authtoken  
✅ Работает сразу  
✅ Бесплатный  
✅ Простой в использовании

---

## 🐛 Troubleshooting

### localtunnel не запускается?

```bash
# Попробуйте установить глобально:
npm install -g localtunnel
lt --port 3002
```

### "Invalid Host header"?

Это нормально для первого запуска. Откройте URL в браузере и нажмите "Continue".

### URL изменился после перезапуска?

При каждом перезапуске localtunnel URL может измениться. Нужно:

1. Обновить `.env.local`
2. Запустить `pnpm bot:menu`
3. Обновить в @BotFather

### Хочу постоянный URL?

Используйте ngrok с authtoken (требует регистрацию):

```bash
ngrok authtoken YOUR_TOKEN
ngrok http 3002
```

---

## 🚀 Готово!

Теперь все должно работать! Mini App откроется через Menu Button в Telegram! 🎉
