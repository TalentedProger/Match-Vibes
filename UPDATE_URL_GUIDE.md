# 🔄 Обновление URL после смены Vercel проекта

**Новый URL:** `https://matchvibesmain.vercel.app/`

---

## 📍 3 места где нужно обновить URL

### ✅ Место 1: Локальный .env.local (для разработки)

**Файл:** `.env.local` (в корне проекта)

**Найдите строку:**

```env
NEXT_PUBLIC_APP_URL=https://старый-url.vercel.app
```

**Замените на:**

```env
NEXT_PUBLIC_APP_URL=https://matchvibesmain.vercel.app
```

**Зачем:** Для локальной разработки с ngrok

---

### ✅ Место 2: Vercel Environment Variables (КРИТИЧНО!)

**Это САМОЕ ВАЖНОЕ!** Vercel использует свои переменные, а не .env.local

#### Шаги:

1. **Откройте:** https://vercel.com/dashboard
2. **Найдите проект:** matchvibesmain (или как он называется)
3. **Откройте:** Settings → Environment Variables
4. **Найдите:** `NEXT_PUBLIC_APP_URL`
5. **Кликните:** на переменную → Edit
6. **Замените значение на:**
   ```
   https://matchvibesmain.vercel.app
   ```
7. **Сохраните**

#### Redeploy (ОБЯЗАТЕЛЬНО!):

После изменения переменной:

1. Перейдите в **Deployments**
2. Найдите последний деплоймент
3. Нажмите три точки **...** → **Redeploy**
4. Дождитесь завершения (~2 минуты)

**Почему Redeploy обязателен:**

- Environment Variables применяются только при новом деплое
- Без Redeploy приложение будет использовать старое значение

---

### ✅ Место 3: Telegram Bot Menu Button

**Откройте @BotFather в Telegram**

Отправьте команды:

```
/setmenubutton
```

1. Выберите ваш бот из списка
2. Когда попросит URL, вставьте:
   ```
   https://matchvibesmain.vercel.app
   ```
3. Готово!

---

## 🔄 Как это работает

### Локальная разработка (localhost):

```
.env.local → Next.js читает → Использует для разработки
```

**Пример:**

```env
NEXT_PUBLIC_APP_URL=https://abc123.ngrok.io
```

### Production (Vercel):

```
Vercel Environment Variables → Next.js на Vercel → Использует в production
```

**Пример:**

```
NEXT_PUBLIC_APP_URL=https://matchvibesmain.vercel.app
```

### Telegram Bot:

```
@BotFather Menu Button URL → Telegram открывает → Пользователь видит
```

---

## 🎯 Полная последовательность действий

### Шаг 1: Обновите .env.local (локально)

1. Откройте файл `.env.local` в редакторе
2. Найдите `NEXT_PUBLIC_APP_URL=...`
3. Замените на `https://matchvibesmain.vercel.app`
4. Сохраните файл

### Шаг 2: Обновите Vercel Environment Variables

1. https://vercel.com/dashboard
2. Ваш проект → Settings → Environment Variables
3. `NEXT_PUBLIC_APP_URL` → Edit
4. Значение: `https://matchvibesmain.vercel.app`
5. Save
6. **Deployments → Redeploy последний деплоймент**

### Шаг 3: Обновите @BotFather

1. Откройте @BotFather в Telegram
2. `/setmenubutton`
3. Выберите бот
4. URL: `https://matchvibesmain.vercel.app`

### Шаг 4: Проверьте

1. Откройте ваш бот в Telegram
2. Нажмите Menu Button (кнопка снизу)
3. Должно открыть Mini App с новым URL ✅

---

## ❓ Частые вопросы

### Q: Почему нужно обновлять в 3 местах?

**A:** Каждое место используется в разных ситуациях:

- **`.env.local`** - для локальной разработки на вашем компьютере
- **Vercel Variables** - для production на Vercel (САМОЕ ВАЖНОЕ!)
- **@BotFather** - URL который Telegram открывает при клике на Menu Button

### Q: Если я обновлю только .env.local, заработает?

**A:** ❌ НЕТ!

`.env.local` НЕ попадает на Vercel (он в .gitignore).
Vercel использует СВОИ Environment Variables из Dashboard.

### Q: Нужно ли делать git commit после изменения .env.local?

**A:** ❌ НЕТ!

`.env.local` в `.gitignore` - он НЕ попадает в Git.
Это правильно для безопасности (там секретные ключи).

### Q: Почему нужен Redeploy после изменения Vercel Variables?

**A:** Environment Variables применяются только при BUILD.

Без Redeploy приложение продолжит использовать старые значения из предыдущего билда.

### Q: Как проверить что URL обновился?

**A:** Откройте в браузере:

```
https://matchvibesmain.vercel.app
```

Должна открыться главная страница MatchVibe.

---

## 🐛 Troubleshooting

### Проблема: После обновления все равно 404

**Решение:**

1. Убедитесь что сделали **Redeploy** в Vercel
2. Проверьте что в Vercel Variables правильный URL
3. Очистите кеш Telegram:
   - Закройте Mini App
   - Закройте Telegram полностью
   - Откройте заново

### Проблема: В Vercel нет переменной NEXT_PUBLIC_APP_URL

**Решение:**

1. Settings → Environment Variables
2. Нажмите **Add New**
3. Key: `NEXT_PUBLIC_APP_URL`
4. Value: `https://matchvibesmain.vercel.app`
5. Environment: **Production** (галочка)
6. Save
7. Redeploy

---

## 📋 Чек-лист обновления

- [ ] `.env.local` обновлен (для локальной разработки)
- [ ] Vercel Environment Variables обновлены
- [ ] Redeploy выполнен в Vercel
- [ ] Redeploy завершился успешно (✅ Ready)
- [ ] @BotFather Menu Button обновлен
- [ ] Проверено в браузере: `https://matchvibesmain.vercel.app` открывается
- [ ] Проверено в Telegram: Mini App открывается

---

## 🎯 Итого

**Главное правило:**

> `.env.local` - только для локальной разработки  
> **Vercel Environment Variables** - для production (КРИТИЧНО!)  
> **@BotFather Menu Button** - что видит пользователь

**После изменения Vercel Variables ВСЕГДА делайте Redeploy!**

---

## 🚀 Быстрая команда для обновления

После того как обновили все 3 места, можете сделать commit для документирования:

```bash
git add .
git commit -m "docs: обновлен URL на https://matchvibesmain.vercel.app"
git push origin main
```

Это НЕ обновит URL (он уже обновлен в Vercel), но задокументирует изменение.
