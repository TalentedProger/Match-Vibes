# Исправление ошибки 404: DEPLOYMENT_NOT_FOUND

**Ошибка:**

```
404: NOT_FOUND
Code: DEPLOYMENT_NOT_FOUND
ID: fra1::tz7sd-1762621238058-454e6b1da31d
```

**Что это значит:**  
Vercel не может найти ваш деплоймент. Это НЕ ошибка кода, а проблема с Vercel.

---

## 🔍 Возможные причины

1. ❌ **Деплоймент был удален** из Vercel Dashboard
2. ❌ **Проект был удален** из Vercel
3. ❌ **Build упал** и деплоймент не создался
4. ❌ **Старая/неправильная ссылка** в Telegram Bot
5. ❌ **Environment Variables** не настроены в Vercel

---

## ✅ Решение по шагам

### Шаг 1: Проверьте проект в Vercel

1. Откройте https://vercel.com/dashboard
2. Найдите проект **MatchVibe** (или как он называется)
3. Если проекта НЕТ → переходите к **Шаг 2: Создать новый деплой**
4. Если проект ЕСТЬ → переходите к **Шаг 3: Проверить деплойменты**

---

### Шаг 2: Создать новый деплой (если проект удален)

#### A. Import проекта заново

1. В Vercel Dashboard нажмите **Add New** → **Project**
2. Выберите **Import Git Repository**
3. Выберите репозиторий `TalentedProger/Match-Vibes`
4. Нажмите **Import**

#### B. Настройте Environment Variables

**КРИТИЧЕСКИ ВАЖНО!** Добавьте все переменные:

1. В настройках проекта → **Settings** → **Environment Variables**
2. Добавьте каждую переменную:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=your-bot-token
NEXT_PUBLIC_BOT_USERNAME=your_bot_username
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
```

**Важно:**

- Для каждой переменной выберите **Environment: Production**
- Скопируйте значения из вашего `.env.local`

#### C. Deploy

1. Нажмите **Deploy**
2. Дождитесь завершения (2-3 минуты)
3. Получите новый URL (например: `https://match-vibes.vercel.app`)

---

### Шаг 3: Проверить деплойменты (если проект существует)

1. В Vercel Dashboard откройте ваш проект
2. Перейдите на вкладку **Deployments**
3. Проверьте последний деплоймент:

#### Если последний деплоймент ✅ Ready:

**Проблема:** Старая ссылка в Telegram Bot

**Решение:**

1. Скопируйте **новый URL** из Vercel (напр. `https://match-vibes-xyz.vercel.app`)
2. Обновите в `.env.local`:
   ```env
   NEXT_PUBLIC_APP_URL=https://match-vibes-xyz.vercel.app
   ```
3. Обновите в **Vercel Environment Variables**:
   - Settings → Environment Variables
   - Найдите `NEXT_PUBLIC_APP_URL`
   - Замените на новый URL
4. Обновите Menu Button:
   ```bash
   pnpm bot:menu
   ```
5. Обновите @BotFather:
   - Отправьте `/setmenubutton` в @BotFather
   - Выберите ваш бот
   - Установите URL: `https://match-vibes-xyz.vercel.app`

#### Если последний деплоймент ❌ Failed:

**Проблема:** Build упал

**Решение:**

1. Кликните на Failed deployment
2. Откройте **Build Logs**
3. Найдите ошибку (обычно в конце)
4. Исправьте ошибку локально
5. Сделайте commit и push:
   ```bash
   git add .
   git commit -m "fix: исправление ошибки билда"
   git push origin main
   ```
6. Vercel автоматически создаст новый деплоймент

#### Если деплойментов НЕТ:

**Проблема:** Auto-deploy не настроен

**Решение:**

1. Settings → Git
2. Проверьте что **Git Repository** подключен
3. Установите **Production Branch**: `main`
4. Включите **Auto-deployments**
5. Сделайте push для триггера:
   ```bash
   git commit --allow-empty -m "trigger: новый деплой"
   git push origin main
   ```

---

### Шаг 4: Проверить Environment Variables

**Обязательные переменные в Vercel:**

1. Settings → Environment Variables
2. Убедитесь что ВСЕ эти переменные добавлены:

```
✅ NEXT_PUBLIC_SUPABASE_URL
✅ NEXT_PUBLIC_SUPABASE_ANON_KEY
✅ NEXT_PUBLIC_TELEGRAM_BOT_TOKEN
✅ NEXT_PUBLIC_BOT_USERNAME
✅ NEXT_PUBLIC_APP_URL
```

**Если переменных нет:**

1. Добавьте каждую вручную
2. После добавления нажмите **Redeploy** в Deployments

---

### Шаг 5: Форсировать новый деплой

Если все остальное не помогло:

```bash
# 1. Создайте пустой коммит для триггера
git commit --allow-empty -m "deploy: форсированный редеплой"

# 2. Запушьте
git push origin main

# 3. Откройте Vercel Dashboard
# Вы увидите новый деплоймент запустился
```

---

## 🧪 Проверка после исправления

### 1. Проверьте деплоймент в Vercel

- Откройте Vercel Dashboard → Deployments
- Последний деплоймент должен быть ✅ Ready
- Скопируйте URL (напр. `https://match-vibes.vercel.app`)

### 2. Откройте URL в браузере

```
https://your-app.vercel.app
```

**Должно показать:** Главную страницу MatchVibe

### 3. Проверьте в Telegram

1. Откройте ваш бот в Telegram
2. Нажмите на Menu Button (кнопка снизу)
3. Должно открыть Mini App

---

## 🚨 Частые ошибки

### Ошибка: "Application error: a client-side exception has occurred"

**Причина:** Отсутствуют Environment Variables

**Решение:**

1. Vercel → Settings → Environment Variables
2. Добавьте все переменные из `.env.local`
3. Redeploy

### Ошибка: "PGRST205: Could not find the table"

**Причина:** SQL схема не выполнена в Supabase

**Решение:**

1. Откройте Supabase → SQL Editor
2. Скопируйте весь SQL из `Docs/supabase_schema.sql`
3. Выполните (Run)
4. Redeploy в Vercel

### Ошибка: Build Failed - TypeScript errors

**Причина:** TypeScript ошибки

**Решение:**
Уже настроено в `next.config.js`:

```javascript
typescript: {
  ignoreBuildErrors: true,
}
```

Но лучше исправить ошибки локально:

```bash
pnpm type-check
```

---

## 📋 Чек-лист исправления

- [ ] Проект существует в Vercel Dashboard
- [ ] Последний деплоймент ✅ Ready
- [ ] Environment Variables настроены в Vercel
- [ ] `NEXT_PUBLIC_APP_URL` соответствует актуальному Vercel URL
- [ ] Menu Button обновлен с новым URL
- [ ] @BotFather Menu Button URL обновлен
- [ ] Приложение открывается в браузере
- [ ] Mini App открывается из Telegram

---

## 🎯 Быстрое решение (90% случаев)

**Самая частая причина:** Старая ссылка в Telegram Bot

**Решение за 2 минуты:**

1. Откройте Vercel Dashboard → ваш проект
2. Скопируйте актуальный URL (напр. `https://match-vibes-abc.vercel.app`)
3. Обновите @BotFather:
   ```
   /setmenubutton → Select bot → URL: https://match-vibes-abc.vercel.app
   ```
4. Готово! Проверьте в Telegram

---

## 📞 Если ничего не помогло

1. Проверьте Build Logs в Vercel
2. Проверьте Function Logs в Vercel
3. Попробуйте создать новый проект в Vercel
4. Проверьте что GitHub репозиторий доступен

**Лог-файлы:**

- Vercel → Deployments → [Latest] → Build Logs
- Vercel → Deployments → [Latest] → Function Logs
