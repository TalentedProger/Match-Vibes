# 🚀 Supabase Setup Guide for MatchVibe

**Status:** Required for Authentication  
**Время выполнения:** ~10 минут  
**Последнее обновление:** 2025-01-08

---

## 📋 Что нужно сделать

1. ✅ Создать проект в Supabase
2. ✅ Выполнить SQL схему базы данных
3. ✅ Получить API ключи
4. ✅ Настроить переменные окружения
5. ✅ Очистить кэш базы данных (если нужно)

---

## 🔧 Шаг 1: Создание проекта Supabase

### 1.1 Зарегистрироваться/Войти

1. Откройте [supabase.com](https://supabase.com)
2. Нажмите **"Start your project"**
3. Войдите через GitHub или email

### 1.2 Создать новый проект

1. Нажмите **"New project"**
2. Заполните данные:
   - **Name:** `matchvibe` (или любое имя)
   - **Database Password:** Придумайте надежный пароль (сохраните его!)
   - **Region:** Выберите ближайший регион (например, `Europe (Frankfurt)`)
   - **Pricing Plan:** Free tier (для тестирования)
3. Нажмите **"Create new project"**
4. ⏳ Подождите 1-2 минуты пока проект создастся

---

## 📊 Шаг 2: Выполнение SQL схемы

### 2.1 Открыть SQL Editor

1. В левом меню выберите **"SQL Editor"**
2. Нажмите **"New query"**

### 2.2 Скопировать схему

**⚠️ ИСПОЛЬЗУЙТЕ БЕЗОПАСНУЮ ВЕРСИЮ СХЕМЫ:**

1. Откройте файл `/Docs/supabase_schema_safe.sql` в вашем проекте
2. Скопируйте **весь** SQL код (232 строки)
3. Вставьте в SQL Editor в Supabase

> **Примечание:** Используйте `supabase_schema_safe.sql` вместо `supabase_schema.sql` - эта версия безопасна для повторного выполнения и не даст ошибок если таблицы уже частично созданы.

### 2.3 Выполнить запрос

1. Нажмите **"Run"** (или `Ctrl+Enter`)
2. ✅ Должно появиться сообщение: `Success. No rows returned`
3. Если ошибка - проверьте, что скопировали весь код

### 2.4 Проверить таблицы

1. В левом меню выберите **"Table Editor"**
2. Вы должны увидеть таблицы:
   - ✅ `profiles`
   - ✅ `categories`
   - ✅ `questions`
   - ✅ `rooms`
   - ✅ `responses`
   - ✅ `results`
   - ✅ `favorites`
   - ✅ `achievements`
   - ✅ `user_achievements`

---

## 🔑 Шаг 3: Получение API ключей

### 3.1 Открыть настройки API

1. В левом меню выберите **"Settings"** (⚙️)
2. Выберите **"API"**

### 3.2 Скопировать ключи

Вам нужны два значения:

#### **Project URL**

```
https://xxxxxxxxxx.supabase.co
```

- Находится в разделе **"Configuration"** → **"URL"**
- Скопируйте полностью (включая `https://`)

#### **anon / public key**

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSI...
```

- Находится в разделе **"Project API keys"** → **"anon public"**
- Скопируйте полностью (это длинный JWT токен)

> ⚠️ **НЕ копируйте `service_role` ключ!** Он дает полный доступ к БД.

---

## 🌍 Шаг 4: Настройка переменных окружения

### 4.1 Локальная разработка (.env.local)

1. Скопируйте `.env.example` → `.env.local`:

   ```bash
   copy .env.example .env.local
   ```

2. Откройте `.env.local` и заполните:

   ```env
   NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxx.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
   ```

3. ✅ Сохраните файл

### 4.2 Vercel Deployment

1. Откройте [vercel.com](https://vercel.com)
2. Перейдите в ваш проект → **Settings** → **Environment Variables**
3. Добавьте переменные:

| Name                            | Value                            | Environment                      |
| ------------------------------- | -------------------------------- | -------------------------------- |
| `NEXT_PUBLIC_SUPABASE_URL`      | `https://xxxxxxxxxx.supabase.co` | Production, Preview, Development |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJhbGciOiJIUzI1NiI...`         | Production, Preview, Development |

4. Нажмите **"Save"**
5. **Redeploy** проект:
   - Перейдите в **"Deployments"**
   - Нажмите **"⋯"** → **"Redeploy"** на последнем деплойменте
   - ✅ Подождите пока завершится

---

## 🗑️ Шаг 5: Очистка базы данных (если нужно)

### Когда это нужно?

- Если хотите удалить всех тестовых пользователей
- Если что-то пошло не так и нужно начать с чистого листа

### 5.1 Очистить таблицу profiles

1. В Supabase → **SQL Editor** → **New query**
2. Выполните команду:
   ```sql
   DELETE FROM profiles;
   ```
3. ✅ Все пользователи удалены

### 5.2 Очистить все таблицы (осторожно!)

```sql
-- Удалить все данные, но сохранить структуру
TRUNCATE profiles, rooms, responses, results, favorites, user_achievements CASCADE;

-- Категории и достижения восстановятся автоматически
-- или можно заново выполнить INSERT из schema
```

> ⚠️ **Внимание:** Это удалит ВСЕ данные безвозвратно!

---

## ✅ Проверка настройки

### Тест 1: Проверить переменные

```bash
# В терминале проекта
echo $NEXT_PUBLIC_SUPABASE_URL
# Должен вывести ваш URL
```

### Тест 2: Проверить подключение

1. Запустите проект: `pnpm dev`
2. Откройте Mini App через Telegram
3. В консоли браузера (F12) не должно быть ошибок про Supabase
4. ✅ Если видите `[Auth API] Telegram data validated successfully` - всё работает!

### Тест 3: Проверить создание пользователя

1. Авторизуйтесь через Telegram Mini App
2. В Supabase → **Table Editor** → **profiles**
3. ✅ Должен появиться новый пользователь с вашим `telegram_id`

---

## 🐛 Troubleshooting

### Ошибка: "trigger 'update_profiles_updated_at' already exists"

**Причина:** Вы пытаетесь выполнить старую версию схемы повторно

**Решение:**

1. ✅ Используйте **безопасную версию**: `/Docs/supabase_schema_safe.sql`
2. Этот файл можно запускать многократно без ошибок
3. Он автоматически удаляет старые триггеры/политики перед созданием новых
4. Скопируйте весь код из `supabase_schema_safe.sql` и выполните в SQL Editor

**Альтернатива (очистить всё и начать заново):**

```sql
-- В SQL Editor выполните:
DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
GRANT ALL ON SCHEMA public TO postgres;
GRANT ALL ON SCHEMA public TO public;

-- Затем выполните supabase_schema_safe.sql
```

---

### Ошибка: "Could not find the table 'public.profiles'"

**Причина:** SQL схема не выполнена или выполнена с ошибками

**Решение:**

1. Откройте Supabase → **Table Editor**
2. Проверьте наличие таблицы `profiles`
3. Если нет - выполните `/Docs/supabase_schema_safe.sql` (Шаг 2)

---

### Ошибка: "Your project's URL and Key are required"

**Причина:** Переменные окружения не настроены

**Решение:**

1. Проверьте `.env.local` (для локальной разработки)
2. Проверьте Vercel Environment Variables (для production)
3. Убедитесь что переменные начинаются с `NEXT_PUBLIC_`
4. Перезапустите dev сервер или redeploy на Vercel

---

### Ошибка: "Invalid JWT token"

**Причина:** Неправильный `ANON_KEY` или истек токен

**Решение:**

1. Проверьте что скопировали **anon** ключ (не service_role)
2. Убедитесь что скопировали ключ полностью (он очень длинный)
3. Попробуйте пересоздать проект Supabase

---

### Ошибка: RLS policy violation

**Причина:** Устаревшая схема с auth.uid() политиками

**Решение:**

1. Откройте Supabase → **SQL Editor**
2. Удалите старые политики:
   ```sql
   DROP POLICY IF EXISTS "Public profiles are viewable by everyone" ON profiles;
   DROP POLICY IF EXISTS "Users can update own profile" ON profiles;
   DROP POLICY IF EXISTS "Users can insert own profile" ON profiles;
   -- Повторите для всех таблиц
   ```
3. Выполните обновленную схему из `/Docs/supabase_schema.sql`

---

## 📚 Полезные ссылки

- [Supabase Dashboard](https://supabase.com/dashboard)
- [Supabase Docs - Row Level Security](https://supabase.com/docs/guides/auth/row-level-security)
- [Supabase Docs - API Settings](https://supabase.com/docs/guides/api)
- [PostgreSQL Documentation](https://www.postgresql.org/docs/)

---

## 🎯 Чеклист завершения

Перед тем как продолжить, убедитесь:

- [ ] Проект Supabase создан
- [ ] SQL схема выполнена успешно
- [ ] 9 таблиц видны в Table Editor
- [ ] API ключи скопированы
- [ ] `.env.local` настроен (для локальной разработки)
- [ ] Vercel Environment Variables настроены (для production)
- [ ] Проект redeploy-нут на Vercel
- [ ] Тестовая авторизация через Telegram работает
- [ ] Пользователь появляется в таблице `profiles`

---

**✅ Готово!** Теперь аутентификация должна работать корректно.

**Следующий шаг:** Протестируйте Mini App на мобильном устройстве через Telegram.

---

**Вопросы?** Проверьте:

- `/Docs/Bug_tracking.md` - известные проблемы
- `/VERCEL_DEPLOYMENT.md` - инструкции по деплою
- Console в браузере (F12) - логи ошибок
