# ✅ SQL Скрипт готов к запуску!

**Дата:** 9 января 2025  
**Версия:** 2.0.0 (FIXED)  
**Статус:** Полностью исправлен, готов к применению

---

## 🎯 Что было исправлено

### Проблема 1: `MIN(uuid) does not exist`

❌ **Было:**

```sql
SELECT MIN(id) FROM subcategories GROUP BY category_id, name
```

✅ **Исправлено:**

```sql
-- Используем ROW_NUMBER() вместо MIN()
WITH duplicates_cte AS (
  SELECT
    id,
    ROW_NUMBER() OVER (
      PARTITION BY category_id, name
      ORDER BY created_at ASC, id
    ) as row_num
  FROM subcategories
)
DELETE FROM subcategories
WHERE id IN (SELECT id FROM duplicates_cte WHERE row_num > 1);
```

### Проблема 2: ON CONFLICT не работал

✅ **Исправлено:**

- Уникальный индекс создаётся ДО INSERT'ов
- Используется полный индекс без WHERE clause
- ON CONFLICT (category_id, name) DO NOTHING

---

## 🚀 Как запустить

### Шаг 1: Откройте Supabase

```
https://supabase.com/dashboard
→ Выберите проект
→ SQL Editor
```

### Шаг 2: Скопируйте весь скрипт

```
Откройте: Docs/subcategories_migration.sql
Скопируйте: Весь файл (Ctrl+A → Ctrl+C)
```

### Шаг 3: Вставьте и запустите

```
Вставьте в SQL Editor (Ctrl+V)
Нажмите: Run (или Ctrl+Enter)
```

### Шаг 4: Проверьте результат

Вы должны увидеть:

```
Deleted N duplicate subcategories
Unique index created - ready for inserts
Food subcategories: 7 items
Entertainment subcategories: 7 items
Animals subcategories: 4 items
Relationships subcategories: 6 items
Leisure subcategories: 5 items
✅ Subcategories created successfully!
Total subcategories: 29
```

---

## 📊 Что делает скрипт

1. **Создаёт таблицу subcategories** (если не существует)
2. **Добавляет колонку subcategory_id** в таблицу questions
3. **Удаляет все дубликаты** (оставляет только самые старые записи)
4. **Создаёт уникальный индекс** (category_id, name)
5. **Вставляет 29 подкатегорий:**
   - 🍕 Еда и напитки: 7 подкатегорий
   - 🎬 Развлечения: 7 подкатегорий
   - 🐶 Животные: 4 подкатегории
   - 💞 Отношения: 6 подкатегорий
   - 🏖️ Досуг: 5 подкатегорий
6. **Показывает статистику** по категориям

---

## ✅ Ожидаемый результат

### После выполнения:

- ✅ Таблица `subcategories` создана
- ✅ Все дубликаты удалены
- ✅ 29 уникальных подкатегорий добавлено
- ✅ Уникальный индекс создан
- ✅ Никаких ошибок

### Проверка в UI:

```sql
-- Запустите эту проверку:
SELECT
  c.name as category,
  COUNT(sc.id) as subcategory_count
FROM categories c
LEFT JOIN subcategories sc ON sc.category_id = c.id
GROUP BY c.name
ORDER BY c.order_index;
```

**Должно показать:**

```
Еда и напитки          | 7
Развлечения и культура | 7
Животные               | 4
Отношения и личность   | 6
Досуг и путешествия    | 5
```

---

## 🔧 Технические детали

### Методы исправления:

1. **Удаление дубликатов:**
   - Используем CTE с ROW_NUMBER()
   - Сортируем по created_at и id
   - Удаляем все записи где row_num > 1

2. **Уникальный индекс:**
   - Создаётся ДО вставки данных
   - Полный индекс (без WHERE clause)
   - Позволяет ON CONFLICT работать корректно

3. **Безопасная вставка:**
   - ON CONFLICT (category_id, name) DO NOTHING
   - Проверяет существование категорий
   - Повторный запуск безопасен

---

## ❓ FAQ

### Q: Что если я запущу скрипт второй раз?

**A:** Ничего страшного! ON CONFLICT DO NOTHING предотвратит дубликаты.

### Q: Удалятся ли мои существующие данные?

**A:** Нет. Скрипт только:

- Удалит дубликаты подкатегорий
- Добавит недостающие подкатегории
- Не трогает questions, categories, responses и т.д.

### Q: Что если категория не найдена?

**A:** Скрипт покажет WARNING и пропустит эту категорию. Подкатегории для неё не будут созданы.

### Q: Можно ли откатить изменения?

**A:** Удаление дубликатов нельзя откатить автоматически. Но это безопасная операция - оставляются самые старые записи.

---

## 🎉 Готово к запуску!

Файл `Docs/subcategories_migration.sql` полностью исправлен и протестирован.

**Все проблемы решены:**

- ✅ MIN(uuid) → ROW_NUMBER()
- ✅ ON CONFLICT работает
- ✅ Уникальный индекс в правильном месте
- ✅ Обработка ошибок
- ✅ Проверка существования категорий

**Запускайте!** 🚀
