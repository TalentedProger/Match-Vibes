# 🔧 ФИНАЛЬНОЕ ИСПРАВЛЕНИЕ WEBHOOK SECRET

**Дата:** 9 января 2025, 17:25  
**Проблема:** Invalid webhook secret token (401 ошибки)  
**Причина:** Токен содержит недопустимые символы

---

## ✅ НОВЫЙ ПРАВИЛЬНЫЙ ТОКЕН

```
rvR2cW3tfRZbQjMtRg4rX5OOYxWhbNY3
```

**Этот токен валидный!** Содержит только разрешённые символы: `A-Z, a-z, 0-9, _, -`

---

## 🚀 ПОШАГОВОЕ ИСПРАВЛЕНИЕ

### Шаг 1: Обновите .env.local

Откройте (или создайте) файл `.env.local` в корне проекта:

```bash
# Другие переменные...
TELEGRAM_WEBHOOK_SECRET=rvR2cW3tfRZbQjMtRg4rX5OOYxWhbNY3
```

**Сохраните файл!**

---

### Шаг 2: Обновите Vercel Environment Variables

1. Откройте: https://vercel.com/dashboard
2. Выберите проект **MatchVibe**
3. Settings → Environment Variables
4. **НАЙДИТЕ** существующую переменную `TELEGRAM_WEBHOOK_SECRET`
5. **УДАЛИТЕ** её (кнопка с тремя точками → Delete)
6. **СОЗДАЙТЕ НОВУЮ:**
   - **Name:** `TELEGRAM_WEBHOOK_SECRET`
   - **Value:** `rvR2cW3tfRZbQjMtRg4rX5OOYxWhbNY3`
   - **Environments:** ✅ Production, ✅ Preview, ✅ Development
7. **Save**

---

### Шаг 3: Redeploy в Vercel

**ВАЖНО:** После изменения env переменных нужен redeploy!

1. Откройте: https://vercel.com/dashboard/deployments
2. Найдите последний деплоймент
3. Нажмите на три точки `...` → **Redeploy**
4. Подтвердите redeploy

**Или через CLI:**

```bash
vercel --prod
```

**Дождитесь завершения деплоя** (2-3 минуты)

---

### Шаг 4: Переустановите webhook

```bash
pnpm bot:webhook
```

**Ожидаемый результат:**

```
🔧 Setting Telegram webhook...
📍 Webhook URL: https://matchvibesmain.vercel.app/api/bot/webhook
🔒 Using webhook secret: rvR2...bNY3
✅ Webhook set successfully!
{ ok: true, result: true, description: 'Webhook was set' }

📊 Getting webhook info...
✅ Webhook info:
{
  "url": "https://matchvibesmain.vercel.app/api/bot/webhook",
  "has_custom_certificate": false,
  "pending_update_count": 0,
  ...
}
```

**Если видите ошибку "unallowed characters"** - проверьте что скопировали токен правильно (без пробелов, без переносов строк).

---

### Шаг 5: Проверка

#### В консоли скрипта:

```
✅ Webhook set successfully!
```

#### В Telegram боте:

Напишите боту `/start` - должен ответить

#### В Vercel логах:

- ❌ **Было:** `Invalid webhook secret token` (401)
- ✅ **Стало:** `200 OK` или `Webhook POST received`

---

## 🔍 Диагностика если не работает

### Проблема 1: "unallowed characters"

**Причина:** Токен содержит недопустимые символы

**Решение:**

1. Сгенерируйте новый токен:
   ```bash
   powershell -ExecutionPolicy Bypass -File scripts\generate-webhook-secret.ps1
   ```
2. Повторите шаги 1-4

---

### Проблема 2: Всё ещё 401 ошибки

**Причина:** Vercel не получил новую переменную

**Решение:**

1. Проверьте что переменная добавлена: Vercel → Settings → Environment Variables
2. **ОБЯЗАТЕЛЬНО сделайте Redeploy** (Шаг 3)
3. Подождите 2-3 минуты после redeploy
4. Повторите `pnpm bot:webhook`

---

### Проблема 3: Токены не совпадают

**Причина:** Разные токены в .env.local и Vercel

**Решение:**

1. Убедитесь что используете **ОДИН И ТОТ ЖЕ** токен:
   - `.env.local`: `rvR2cW3tfRZbQjMtRg4rX5OOYxWhbNY3`
   - Vercel: `rvR2cW3tfRZbQjMtRg4rX5OOYxWhbNY3`
2. Проверьте что нет лишних пробелов/переносов
3. Сделайте redeploy

---

### Проблема 4: "TELEGRAM_WEBHOOK_SECRET not set"

**Причина:** Переменная не загружена в скрипт

**Решение:**

1. Проверьте что файл `.env.local` существует в корне проекта
2. Проверьте что в нём есть строка:
   ```
   TELEGRAM_WEBHOOK_SECRET=rvR2cW3tfRZbQjMtRg4rX5OOYxWhbNY3
   ```
3. Перезапустите терминал
4. Повторите `pnpm bot:webhook`

---

## 📝 Что мы исправили

### 1. Сгенерировали правильный токен

- ✅ Только разрешённые символы: `A-Z, a-z, 0-9, _, -`
- ✅ Длина 32 символа
- ❌ Старый токен: содержал недопустимые символы

### 2. Обновили скрипт set-webhook.ts

- ✅ Передаёт `secret_token` в Telegram
- ✅ Логирует использование токена (с маскировкой)
- ✅ Показывает предупреждение если токен не задан

### 3. Обновили документацию

- ✅ `FIX_WEBHOOK_SECRET.md` - подробная инструкция
- ✅ `WEBHOOK_FIX_FINAL.md` - этот файл
- ✅ `scripts/generate-webhook-secret.ps1` - генератор токена

---

## 🎯 Итоговый чеклист

- [ ] **Шаг 1:** Токен добавлен в `.env.local`
- [ ] **Шаг 2:** Токен обновлён в Vercel (старый удалён, новый добавлен)
- [ ] **Шаг 3:** Сделан Redeploy в Vercel
- [ ] **Шаг 4:** Дождались завершения deploy (2-3 мин)
- [ ] **Шаг 5:** Запустили `pnpm bot:webhook`
- [ ] **Шаг 6:** Проверили что webhook успешно установлен
- [ ] **Шаг 7:** Протестировали бота (отправили `/start`)
- [ ] **Шаг 8:** Проверили Vercel логи (нет ошибок 401)

---

## ✅ После выполнения

Если всё сделано правильно:

✅ **Webhook успешно установлен** с secret токеном  
✅ **Telegram отправляет** токен в заголовке  
✅ **Vercel проверяет** токен корректно  
✅ **Нет ошибок 401** в логах  
✅ **Бот работает** нормально  
✅ **Дополнительная защита** от поддельных запросов

---

## 🆘 Если ничего не помогло

1. **Временно отключите проверку:**
   - Удалите `TELEGRAM_WEBHOOK_SECRET` из Vercel
   - Redeploy
   - Запустите `pnpm bot:webhook` (без токена в .env.local)
   - Бот будет работать БЕЗ проверки secret

2. **Проверьте логи в реальном времени:**

   ```bash
   vercel logs --follow
   ```

3. **Напишите в поддержку** если проблема осталась

---

**ВАЖНО:** Выполняйте шаги СТРОГО ПО ПОРЯДКУ! Особенно важен Redeploy после изменения env переменных.

**Запускайте!** 🚀
