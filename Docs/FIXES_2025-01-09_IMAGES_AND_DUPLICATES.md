# Исправления: Изображения и дубликаты карточек

**Дата:** 2025-01-09  
**Версия:** 1.0

---

## 🐛 Обнаруженные проблемы

### 1. Изображения не отображаются для "Завтрак мечты"

- **Причина:** API не фильтровал вопросы по `subcategory_id`
- **Симптом:** Показывались все вопросы категории вместо конкретной подкатегории

### 2. Карточки дублируются (показываются по 2 раза)

- **Причина:** В базе данных есть дубликаты вопросов
- **Симптом:** Каждая карточка отображается дважды в игре

### 3. Ошибка 404 при отображении результатов

- **Причина:** GET метод `/api/game/[roomId]/calculate` возвращал 404 когда результата еще нет
- **Симптом:** Страница результатов не загружалась

---

## ✅ Внесенные исправления

### Исправление 1: Фильтрация по подкатегориям

#### Обновленные файлы:

1. **`src/app/api/categories/[id]/questions/route.ts`**
   - Добавлена поддержка query параметра `subcategoryId`
   - API теперь фильтрует вопросы по подкатегории

2. **`src/components/room/room-creator.tsx`**
   - Передает `subcategoryId` при создании комнаты

3. **`src/hooks/use-room.ts`**
   - Обновлена сигнатура `createRoom` для поддержки `subcategoryId`

4. **`src/stores/room-store.ts`**
   - Добавлен параметр `subcategoryId` в метод `createRoom`

5. **`src/app/api/rooms/route.ts`**
   - Сохраняет `subcategory_id` при создании комнаты

6. **`src/app/(main)/game/[roomId]/page.tsx`**
   - Использует `subcategory_id` из комнаты при загрузке вопросов

7. **`src/types/room.ts`**
   - Добавлено поле `subcategory_id` в интерфейс `Room`

### Исправление 2: Ошибка 404 в результатах

#### Обновленные файлы:

1. **`src/app/api/game/[roomId]/calculate/route.ts`**
   - GET метод теперь использует `.maybeSingle()` вместо `.single()`
   - Возвращает `{ result: null }` вместо 404 когда результата нет
   - Добавлена фильтрация вопросов по `subcategory_id` при расчете

2. **`src/hooks/use-match-result.ts`**
   - Корректно обрабатывает случай когда `result === null`

---

## 📋 Инструкции по применению

### Шаг 1: Обновите базу данных

Выполните в **Supabase SQL Editor** следующие скрипты **по порядку**:

#### 1.1. Добавьте поле subcategory_id в таблицу rooms

```sql
-- Файл: Docs/add_subcategory_to_rooms.sql
ALTER TABLE rooms
ADD COLUMN IF NOT EXISTS subcategory_id UUID REFERENCES subcategories(id) ON DELETE SET NULL;

CREATE INDEX IF NOT EXISTS idx_rooms_subcategory
ON rooms(subcategory_id);
```

#### 1.2. Проверьте и удалите дубликаты вопросов

```sql
-- Файл: Docs/check_and_fix_duplicates.sql

-- Сначала проверьте наличие дубликатов
SELECT
  q.text,
  c.name as category_name,
  COUNT(*) as duplicate_count
FROM questions q
LEFT JOIN categories c ON c.id = q.category_id
WHERE q.is_active = true
GROUP BY q.text, c.name
HAVING COUNT(*) > 1
ORDER BY duplicate_count DESC;

-- Если есть дубликаты, удалите их (оставив только самые старые)
WITH duplicates_cte AS (
  SELECT
    id,
    ROW_NUMBER() OVER (
      PARTITION BY text, category_id
      ORDER BY created_at ASC, id
    ) as row_num
  FROM questions
  WHERE is_active = true
)
DELETE FROM questions
WHERE id IN (
  SELECT id FROM duplicates_cte WHERE row_num > 1
);
```

#### 1.3. Обновите URL изображений для "Завтрак мечты"

```sql
-- Обновить image_url для вопросов подкатегории "Завтрак мечты"
UPDATE questions
SET image_url = '/images/subcategories/food-dream-breakfast/card-' || order_index || '.jpg'
WHERE subcategory_id = (
  SELECT id FROM subcategories WHERE name = 'Завтрак мечты' LIMIT 1
);
```

### Шаг 2: Закоммитьте и задеплойте изменения

```bash
# Добавьте все изменения
git add .

# Закоммитьте
git commit -m "fix: добавлена фильтрация по подкатегориям, исправлены дубликаты и ошибка 404"

# Отправьте на GitHub
git push origin main
```

Vercel автоматически задеплоит изменения.

### Шаг 3: Проверьте работу

1. **Откройте приложение**
2. **Перейдите:** Еда и напитки → Завтрак мечты
3. **Создайте комнату** и начните игру
4. **Проверьте:**
   - ✅ Отображаются только вопросы подкатегории "Завтрак мечты"
   - ✅ Изображения загружаются из `/images/subcategories/food-dream-breakfast/`
   - ✅ Каждая карточка показывается только 1 раз (нет дубликатов)
   - ✅ Результаты отображаются корректно после завершения игры

---

## 🔍 Проверка изображений

### Убедитесь что файлы на месте:

```
public/images/subcategories/food-dream-breakfast/
├── card-1.jpg   (Панкейки с ягодами)
├── card-2.jpg   (Круассаны)
├── card-3.jpg   (Яичница с беконом)
├── card-4.jpg   (Авокадо тост)
├── card-5.jpg   (Овсянка с фруктами)
├── card-6.jpg   (Бельгийские вафли)
├── card-7.jpg   (Омлет)
├── card-8.jpg   (Гранола с йогуртом)
├── card-9.jpg   (Бейглы с лососем)
├── card-10.jpg  (Французские тосты)
├── card-11.jpg  (Сырники)
├── card-12.jpg  (Смузи боул)
├── card-13.jpg  (Английский завтрак)
├── card-14.jpg  (Шакшука)
└── card-15.jpg  (Блинчики)
```

### Проверка в браузере:

Откройте в браузере:

```
https://ваш-домен.vercel.app/images/subcategories/food-dream-breakfast/card-1.jpg
```

Если изображение загружается - всё работает правильно!

---

## 🎯 Что изменилось в архитектуре

### До исправления:

```
Создание комнаты → categoryId
Загрузка вопросов → WHERE category_id = ?
Результат: Все вопросы категории (дубликаты)
```

### После исправления:

```
Создание комнаты → categoryId + subcategoryId
Загрузка вопросов → WHERE subcategory_id = ? (если есть)
Результат: Только вопросы конкретной подкатегории (без дубликатов)
```

---

## 📊 Схема данных

### Таблица `rooms` (обновлена):

```sql
CREATE TABLE rooms (
  id UUID PRIMARY KEY,
  host_id UUID REFERENCES profiles(id),
  guest_id UUID REFERENCES profiles(id),
  category_id UUID REFERENCES categories(id),
  subcategory_id UUID REFERENCES subcategories(id), -- НОВОЕ ПОЛЕ
  status TEXT,
  invitation_code TEXT UNIQUE,
  created_at TIMESTAMPTZ,
  started_at TIMESTAMPTZ,
  completed_at TIMESTAMPTZ
);
```

### Таблица `questions`:

```sql
CREATE TABLE questions (
  id UUID PRIMARY KEY,
  category_id UUID REFERENCES categories(id),
  subcategory_id UUID REFERENCES subcategories(id),
  text TEXT NOT NULL,
  image_url TEXT NOT NULL,
  order_index INT,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMPTZ
);
```

---

## 🚨 Важные замечания

1. **Старые комнаты** (созданные до обновления) не будут иметь `subcategory_id` и будут показывать все вопросы категории
2. **Новые комнаты** (после обновления) будут корректно фильтровать вопросы по подкатегории
3. **Дубликаты** удаляются безвозвратно - убедитесь что сделали бэкап БД перед выполнением скрипта
4. **Изображения** должны быть закоммичены в Git и задеплоены на Vercel

---

## ✅ Чеклист проверки

- [ ] Выполнен SQL скрипт `add_subcategory_to_rooms.sql`
- [ ] Выполнен SQL скрипт `check_and_fix_duplicates.sql`
- [ ] Обновлены URL изображений для "Завтрак мечты"
- [ ] Код закоммичен и отправлен на GitHub
- [ ] Vercel успешно задеплоил изменения
- [ ] Изображения отображаются в игре
- [ ] Карточки не дублируются
- [ ] Результаты отображаются корректно
- [ ] Проверены все подкатегории (не только "Завтрак мечты")

---

## 🔧 Отладка

### Если изображения не отображаются:

1. Проверьте что файлы существуют в `public/images/subcategories/food-dream-breakfast/`
2. Проверьте что файлы закоммичены в Git
3. Проверьте что деплой на Vercel прошел успешно
4. Откройте DevTools → Network и проверьте статус загрузки изображений
5. Проверьте URL в базе данных:
   ```sql
   SELECT text, image_url FROM questions
   WHERE subcategory_id = (SELECT id FROM subcategories WHERE name = 'Завтрак мечты');
   ```

### Если карточки дублируются:

1. Проверьте что SQL скрипт удаления дубликатов выполнен
2. Проверьте количество вопросов:
   ```sql
   SELECT COUNT(*) FROM questions
   WHERE subcategory_id = (SELECT id FROM subcategories WHERE name = 'Завтрак мечты');
   ```
   Должно быть ровно 15 вопросов.

### Если ошибка 404 в результатах:

1. Проверьте что код обновлен и задеплоен
2. Проверьте логи Vercel на наличие ошибок
3. Проверьте что оба игрока завершили игру

---

**Готово!** Все три проблемы исправлены. 🎉
