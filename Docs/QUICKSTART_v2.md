# Quick Start Guide - v2 Updates

Быстрый старт для применения изменений по категориям и боту.

---

## 🚀 Шаг 1: Установка зависимостей (30 сек)

```bash
pnpm install
```

Это установит `concurrently` для одновременного запуска Next.js и бота.

---

## 📊 Шаг 2: Обновление базы данных (2 мин)

### В Supabase SQL Editor:

1. Откройте https://supabase.com/
2. Выберите проект
3. Перейдите в **SQL Editor**
4. Создайте **New query**
5. Скопируйте **весь код** из `Docs/subcategories_migration.sql`
6. Нажмите **Run** (Ctrl+Enter)

**Ожидаемый результат:**

```
✅ Subcategories created successfully!
Total subcategories: 39
```

### Проверка:

```sql
SELECT COUNT(*) FROM subcategories;
-- Должно быть 39
```

---

## 🤖 Шаг 3: Настройка бота (1 мин)

### Для локальной разработки:

**Вариант A:** Только Next.js (без бота)

```bash
pnpm dev
```

**Вариант B:** Next.js + Bot вместе

```bash
pnpm dev:all
```

### Для production (после деплоя на Vercel):

```bash
pnpm bot:webhook
```

Или вручную:

```bash
curl -X POST \
  "https://api.telegram.org/bot<ТОКЕН>/setWebhook" \
  -d "url=https://ваш-домен.vercel.app/api/bot/webhook"
```

**Проверка:**

```bash
curl "https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo"
```

---

## ✅ Шаг 4: Проверка (2 мин)

### Тест 1: Категории

1. Откройте http://localhost:3002/categories
2. **Должно быть:** 7 уникальных категорий (без повторов)
3. Кликните на любую категорию
4. **Должна открыться:** страница с подкатегориями
5. Кликните на любую подкатегорию
6. **Должно открыться:** окно создания комнаты

### Тест 2: Бот (в production)

1. Откройте бота в Telegram
2. Отправьте `/start`
3. **Бот должен:** ответить мгновенно с кнопкой Mini App

---

## 📝 Что изменилось

### ✅ Решено:

1. **Категории больше не дублируются** - показываются по 1 разу
2. **Добавлены подкатегории** - 39 подтем в 7 категориях
3. **Бот работает автоматически** - не нужна команда `pnpm bot:dev` в production

### 🆕 Новое:

- Страница подкатегорий `/categories/[id]`
- API endpoints: `GET /api/categories/[id]` и `/subcategories`
- Таблица `subcategories` в БД
- Скрипт `dev-bot.ts` для локальной разработки

---

## 🔧 Команды

### Development:

```bash
pnpm dev          # Только Next.js
pnpm dev:bot      # Только Bot (polling)
pnpm dev:all      # Next.js + Bot одновременно
```

### Production:

```bash
pnpm build        # Сборка
pnpm start        # Запуск production сервера
pnpm bot:webhook  # Настройка webhook для бота
```

### Проверка:

```bash
pnpm bot:check    # Проверить токен бота
```

---

## ⚠️ Частые проблемы

### Проблема: "Subcategories not loading"

**Решение:**

1. Проверьте что миграция применена:

```sql
SELECT COUNT(*) FROM subcategories;
```

2. Должно быть 39 строк

---

### Проблема: "Bot not responding"

**Решение в production:**

```bash
# Проверьте webhook
curl "https://api.telegram.org/bot<ТОКЕН>/getWebhookInfo"

# Переустановите если нужно
pnpm bot:webhook
```

**Решение локально:**

```bash
# Используйте polling mode
pnpm dev:all
```

---

### Проблема: "Categories still duplicated"

**Причина:** Старые данные в БД

**Решение:**

1. Проверьте количество категорий:

```sql
SELECT COUNT(*) FROM categories WHERE is_active = TRUE;
```

2. Должно быть 7 категорий

3. Если больше - удалите дубликаты:

```sql
-- Оставьте только уникальные
DELETE FROM categories
WHERE id NOT IN (
  SELECT MIN(id)
  FROM categories
  GROUP BY name
);
```

---

## 📚 Документация

- **Полная информация:** `Docs/CHANGES_Summary_v2.md`
- **Настройка бота:** `Docs/Bot_Server_Setup.md`
- **Supabase setup:** `Docs/Supabase_Setup_Instructions.md`

---

## 🎯 Готово!

После выполнения всех шагов у вас должно быть:

- ✅ 7 уникальных категорий
- ✅ 39 подкатегорий
- ✅ Бот работает автоматически
- ✅ Подкатегории открываются при клике
- ✅ Комнаты создаются через подкатегории

**Время установки:** ~5 минут  
**Готово к использованию!** 🚀
